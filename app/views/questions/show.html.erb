<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @question.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @question.content %>
</p>
<%= content_tag_for(:li, @answers) do |answer|%>
  <%= answer.title %>
  <%= answer.content %>
<% end %>
<%= render "answers/new"%>
<%= link_to 'Back', questions_path %>

